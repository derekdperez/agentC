# AI: start here — `agentC`

These files are a pre-computed, token-efficient map of this codebase, generated by `ai-usage-hacks.py`. **Read this file first.** Use the maps below to navigate and read exact slices instead of loading whole files — that is the whole point: spend fewer tokens, keep more context free.

Snapshot: **26 Python files**, **~41504 tokens** of source total (counted with tiktoken o200k_base).

## The artifacts

| File | What it is | Use it to… |
| --- | --- | --- |
| `AI_START_HERE.md` | This guide | Understand the workflow |
| `PROJECT_AST.md` | Hotspots + symbol index + per-file map (line numbers, token costs) | Find *where* something is and *what it costs* to read |
| `PROJECT_SKELETON.txt` | `.pyi`-style stubs: signatures + docstrings, `...` bodies | Understand a file's shape cheaply, without its bodies |
| `PROJECT_AST.json` | The same data, machine-readable (incl. `ranking`) | Programmatic queries / tooling |

## Recommended workflow

1. **Read this file.**
2. **Skim the Hotspots table** in `PROJECT_AST.md` — the most depended-on symbols. Reading their skeletons first orients you to the core.
3. **Locate a symbol** via the Symbol index in `PROJECT_AST.md` (`name → path:line`). No blind grepping.
4. **Read the stub** for that file's block in `PROJECT_SKELETON.txt` to learn its API for a fraction of the tokens.
5. **Open real source only when you must edit a body** — and use the line range from the index to read just that slice.

## Core symbols to know (top by importance)

| symbol | kind | refs | location |
| --- | --- | --- | --- |
| `AgentConfig` | class | 8 | `agentc/models.py:19` |
| `_load_json` | function | 7 | `agentc/dashboard.py:50` |
| `WorkflowEngine` | class | 1 | `agentc/engine.py:35` |
| `_engine` | function | 7 | `agentc/cli.py:62` |
| `_systemctl` | function | 5 | `agentc/service.py:23` |
| `CLIAdapter` | class | 6 | `agentc/agents/adapters.py:32` |
| `e` | function | 7 | `agentc/dashboard.py:339` |
| `Event` | class | 6 | `agentc/models.py:128` |
| `_ignored` | function | 2 | `agentc/watcher.py:46` |
| `VariableStore` | class | 2 | `agentc/variables.py:25` |

## Keeping this fresh

These maps are a snapshot. **After changing code, regenerate them** so they don't mislead:

```
python ai-usage-hacks.py
```

If a line number or signature here disagrees with the real source, trust the source and regenerate.
