{
  "name": "design-work",
  "description": "Watch todo/needs_design_work: when a file arrives, the Junior Designer turns its contents into a design document, then the original is archived to completed/.",
  "trigger": {
    "type": "file",
    "path": "todo/needs_design_work",
    "on": "created",
    "pattern": "*",
    "recursive": false
  },
  "actions": [
    {
      "name": "design",
      "type": "agent",
      "agent": "junior-designer",
      "prompt": "A rough idea was dropped into the design queue as the file '${event_filename}'. Turn the idea described below into a complete, developer-ready design document in markdown.",
      "input_file": "${event_path}",
      "capture": "design_document"
    },
    {
      "name": "finalize",
      "type": "python",
      "executable": "scripts/finalize_to_completed.py",
      "args": ["design_document", ".design.md"]
    }
  ],
  "emits": ["design.completed"]
}
